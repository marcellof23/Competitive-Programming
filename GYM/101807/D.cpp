#include <bits/stdc++.h>
using namespace std;

int main()
{
  string str;
  cin >> str;

  stringstream ss(str);
  string num;
  int cnt = 0, temp = -1;
  while (getline(ss, num, 'x'))
  {
    if (cnt % 2 == 0)
    {
    }
    else
    {
    }
    cnt++;
    cout << num << " ";
  }
}